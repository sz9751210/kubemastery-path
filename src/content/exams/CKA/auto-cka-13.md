---
id: auto-cka-13
title: Auto CKA Practice (Batch 13)
category: CKA
duration: 120 mins
---

# Auto CKA Practice (Batch 13)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Scale Deployment
Create a deployment named `resource-tp1jj0` in namespace `default` using image `mysql`.
Scale it to `9` replicas.
Then, perform a rolling update to image `mysql:latest`.


# Task 2: Node Maintenance
Mark node `node-2` as unschedulable (cordon).
Then drain the node, ignoring daemonsets.
Finally, uncordon the node.


# Task 3: Create a Pod
Create a pod named `resource-nrc8nj` in namespace `backend` using image `httpd`.
Ensure it has a label `app=web`.


# Task 4: Scale Deployment
Create a deployment named `resource-6lj610` in namespace `backend` using image `python:3.9`.
Scale it to `3` replicas.
Then, perform a rolling update to image `python:3.9:latest`.


# Task 5: Persistent Volume Claim
Create a PersistentVolumeClaim named `resource-ui1rz5` in namespace `test`.
Request `2Gi` storage with access mode `ReadWriteOnce`.


# Task 6: Expose Service
Expose the deployment `resource-d2u6u0-dep` as a Service named `resource-d2u6u0` in namespace `test`.
The service should listen on port `8691` and be of type `NodePort`.


# Task 7: Persistent Volume Claim
Create a PersistentVolumeClaim named `resource-17awdz` in namespace `frontend`.
Request `1Gi` storage with access mode `ReadWriteMany`.


# Task 8: Persistent Volume Claim
Create a PersistentVolumeClaim named `resource-9i5k7m` in namespace `test`.
Request `1Gi` storage with access mode `ReadWriteMany`.


# Task 9: Create a Pod
Create a pod named `resource-c9zp01` in namespace `kube-system` using image `nginx`.
Ensure it has a label `release=stable`.


# Task 10: Scale Deployment
Create a deployment named `resource-bwjmt4` in namespace `backend` using image `memcached`.
Scale it to `7` replicas.
Then, perform a rolling update to image `memcached:latest`.


# Task 11: Persistent Volume Claim
Create a PersistentVolumeClaim named `resource-cc938o` in namespace `default`.
Request `1Gi` storage with access mode `ReadOnlyMany`.


# Task 12: Node Maintenance
Mark node `node-4` as unschedulable (cordon).
Then drain the node, ignoring daemonsets.
Finally, uncordon the node.


# Task 13: Persistent Volume Claim
Create a PersistentVolumeClaim named `resource-cizd9u` in namespace `dev`.
Request `2Gi` storage with access mode `ReadWriteMany`.


# Task 14: Create a Pod
Create a pod named `resource-o2g3k9` in namespace `frontend` using image `httpd`.
Ensure it has a label `team=blue`.


# Task 15: Create a Pod
Create a pod named `resource-mst5to` in namespace `backend` using image `memcached`.
Ensure it has a label `team=blue`.


# Task 16: Node Maintenance
Mark node `node-1` as unschedulable (cordon).
Then drain the node, ignoring daemonsets.
Finally, uncordon the node.


# Task 17: Scale Deployment
Create a deployment named `resource-a0s9zt` in namespace `default` using image `redis`.
Scale it to `9` replicas.
Then, perform a rolling update to image `redis:latest`.


# Task 18: Node Maintenance
Mark node `node-1` as unschedulable (cordon).
Then drain the node, ignoring daemonsets.
Finally, uncordon the node.


# Task 19: Expose Service
Expose the deployment `resource-79h3o8-dep` as a Service named `resource-79h3o8` in namespace `test`.
The service should listen on port `8019` and be of type `NodePort`.


# Task 20: Scale Deployment
Create a deployment named `resource-v9nowk` in namespace `staging` using image `python:3.9`.
Scale it to `7` replicas.
Then, perform a rolling update to image `python:3.9:latest`.

