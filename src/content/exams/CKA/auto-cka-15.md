---
id: auto-cka-15
title: Auto CKA Practice (Batch 15)
category: CKA
duration: 120 mins
---

# Auto CKA Practice (Batch 15)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Create a Pod
Create a pod named `resource-vyaolo` in namespace `staging` using image `busybox`.
Ensure it has a label `env=prod`.


# Task 2: Expose Service
Expose the deployment `resource-kl5wvr-dep` as a Service named `resource-kl5wvr` in namespace `prod`.
The service should listen on port `6479` and be of type `NodePort`.


# Task 3: Create a Pod
Create a pod named `resource-mci11g` in namespace `dev` using image `redis`.
Ensure it has a label `env=prod`.


# Task 4: Node Maintenance
Mark node `node-5` as unschedulable (cordon).
Then drain the node, ignoring daemonsets.
Finally, uncordon the node.


# Task 5: Scale Deployment
Create a deployment named `resource-pwg16y` in namespace `staging` using image `alpine`.
Scale it to `6` replicas.
Then, perform a rolling update to image `alpine:latest`.


# Task 6: Create a Pod
Create a pod named `resource-5up3aw` in namespace `frontend` using image `busybox`.
Ensure it has a label `tier=frontend`.


# Task 7: Expose Service
Expose the deployment `resource-hg4g03-dep` as a Service named `resource-hg4g03` in namespace `prod`.
The service should listen on port `7699` and be of type `NodePort`.


# Task 8: Create a Pod
Create a pod named `resource-r6plb2` in namespace `backend` using image `node:14`.
Ensure it has a label `tier=frontend`.


# Task 9: Expose Service
Expose the deployment `resource-919hdb-dep` as a Service named `resource-919hdb` in namespace `frontend`.
The service should listen on port `6292` and be of type `ClusterIP`.


# Task 10: Persistent Volume Claim
Create a PersistentVolumeClaim named `resource-n4rq6l` in namespace `kube-system`.
Request `5Gi` storage with access mode `ReadWriteMany`.


# Task 11: Expose Service
Expose the deployment `resource-7qo3xa-dep` as a Service named `resource-7qo3xa` in namespace `test`.
The service should listen on port `4548` and be of type `NodePort`.


# Task 12: Create a Pod
Create a pod named `resource-6snhrd` in namespace `backend` using image `memcached`.
Ensure it has a label `app=web`.


# Task 13: Persistent Volume Claim
Create a PersistentVolumeClaim named `resource-3k7s5p` in namespace `dev`.
Request `1Gi` storage with access mode `ReadWriteOnce`.


# Task 14: Node Maintenance
Mark node `node-5` as unschedulable (cordon).
Then drain the node, ignoring daemonsets.
Finally, uncordon the node.


# Task 15: Create a Pod
Create a pod named `resource-3er8fe` in namespace `kube-system` using image `alpine`.
Ensure it has a label `tier=frontend`.


# Task 16: Node Maintenance
Mark node `node-5` as unschedulable (cordon).
Then drain the node, ignoring daemonsets.
Finally, uncordon the node.


# Task 17: Create a Pod
Create a pod named `resource-cypf58` in namespace `kube-system` using image `memcached`.
Ensure it has a label `tier=frontend`.


# Task 18: Scale Deployment
Create a deployment named `resource-i7otmx` in namespace `frontend` using image `python:3.9`.
Scale it to `10` replicas.
Then, perform a rolling update to image `python:3.9:latest`.


# Task 19: Scale Deployment
Create a deployment named `resource-abi9fx` in namespace `frontend` using image `busybox`.
Scale it to `3` replicas.
Then, perform a rolling update to image `busybox:latest`.


# Task 20: Scale Deployment
Create a deployment named `resource-hzw01q` in namespace `kube-system` using image `node:14`.
Scale it to `10` replicas.
Then, perform a rolling update to image `node:14:latest`.

