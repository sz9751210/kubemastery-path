---
id: auto-ckad-11
title: Auto CKAD Practice (Batch 11)
category: CKAD
duration: 120 mins
---

# Auto CKAD Practice (Batch 11)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Multi-Container Pod
Create a pod named `ckad-72jd1s` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-72jd1s -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-72jd1s -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 2: Multi-Container Pod
Create a pod named `ckad-31fsn3` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-31fsn3 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-31fsn3 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 3: Multi-Container Pod
Create a pod named `ckad-zwzv6f` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-zwzv6f -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-zwzv6f -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 4: Multi-Container Pod
Create a pod named `ckad-3f5p4y` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-3f5p4y -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-3f5p4y -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 5: Multi-Container Pod
Create a pod named `ckad-g9fkee` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-g9fkee -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-g9fkee -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 6: Multi-Container Pod
Create a pod named `ckad-3x33be` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-3x33be -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-3x33be -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 7: Multi-Container Pod
Create a pod named `ckad-5jk5kb` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-5jk5kb -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-5jk5kb -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 8: Multi-Container Pod
Create a pod named `ckad-ky0yww` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ky0yww -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ky0yww -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 9: Multi-Container Pod
Create a pod named `ckad-cxfapf` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-cxfapf -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-cxfapf -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 10: Multi-Container Pod
Create a pod named `ckad-t0x56w` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-t0x56w -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-t0x56w -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 11: Multi-Container Pod
Create a pod named `ckad-x09ct9` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-x09ct9 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-x09ct9 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 12: Multi-Container Pod
Create a pod named `ckad-74v3hd` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-74v3hd -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-74v3hd -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 13: Multi-Container Pod
Create a pod named `ckad-fz7cxa` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-fz7cxa -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-fz7cxa -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 14: Multi-Container Pod
Create a pod named `ckad-wb9033` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-wb9033 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-wb9033 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 15: Multi-Container Pod
Create a pod named `ckad-7u2y1z` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-7u2y1z -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-7u2y1z -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 16: Multi-Container Pod
Create a pod named `ckad-obro8g` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-obro8g -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-obro8g -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 17: Multi-Container Pod
Create a pod named `ckad-5wwsct` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-5wwsct -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-5wwsct -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 18: Multi-Container Pod
Create a pod named `ckad-2gfvlf` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-2gfvlf -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-2gfvlf -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 19: Multi-Container Pod
Create a pod named `ckad-mknh75` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-mknh75 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-mknh75 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 20: Multi-Container Pod
Create a pod named `ckad-hkrgac` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-hkrgac -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-hkrgac -o jsonpath='{.spec.containers[*].name}' | grep c2
```

