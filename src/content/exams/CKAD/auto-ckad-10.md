---
id: auto-ckad-10
title: Auto CKAD Practice (Batch 10)
category: CKAD
duration: 120 mins
---

# Auto CKAD Practice (Batch 10)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Multi-Container Pod
Create a pod named `ckad-ut91bx` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ut91bx -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ut91bx -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 2: Multi-Container Pod
Create a pod named `ckad-olboms` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-olboms -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-olboms -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 3: Multi-Container Pod
Create a pod named `ckad-c4vwfq` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-c4vwfq -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-c4vwfq -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 4: Multi-Container Pod
Create a pod named `ckad-xeegln` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-xeegln -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-xeegln -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 5: Multi-Container Pod
Create a pod named `ckad-ftg9xy` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ftg9xy -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ftg9xy -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 6: Multi-Container Pod
Create a pod named `ckad-uvau2p` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-uvau2p -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-uvau2p -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 7: Multi-Container Pod
Create a pod named `ckad-wlx3es` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-wlx3es -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-wlx3es -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 8: Multi-Container Pod
Create a pod named `ckad-3klstc` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-3klstc -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-3klstc -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 9: Multi-Container Pod
Create a pod named `ckad-x0rr2q` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-x0rr2q -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-x0rr2q -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 10: Multi-Container Pod
Create a pod named `ckad-t22fwx` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-t22fwx -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-t22fwx -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 11: Multi-Container Pod
Create a pod named `ckad-9qbuk8` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-9qbuk8 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-9qbuk8 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 12: Multi-Container Pod
Create a pod named `ckad-4fybbn` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-4fybbn -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-4fybbn -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 13: Multi-Container Pod
Create a pod named `ckad-i4f68e` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-i4f68e -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-i4f68e -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 14: Multi-Container Pod
Create a pod named `ckad-1j5f4r` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-1j5f4r -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-1j5f4r -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 15: Multi-Container Pod
Create a pod named `ckad-c6lzpa` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-c6lzpa -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-c6lzpa -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 16: Multi-Container Pod
Create a pod named `ckad-m5pt73` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-m5pt73 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-m5pt73 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 17: Multi-Container Pod
Create a pod named `ckad-tdn4eo` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-tdn4eo -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-tdn4eo -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 18: Multi-Container Pod
Create a pod named `ckad-46nvw4` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-46nvw4 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-46nvw4 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 19: Multi-Container Pod
Create a pod named `ckad-nb95fz` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-nb95fz -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-nb95fz -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 20: Multi-Container Pod
Create a pod named `ckad-hhp4su` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-hhp4su -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-hhp4su -o jsonpath='{.spec.containers[*].name}' | grep c2
```

