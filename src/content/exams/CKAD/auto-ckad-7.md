---
id: auto-ckad-7
title: Auto CKAD Practice (Batch 7)
category: CKAD
duration: 120 mins
---

# Auto CKAD Practice (Batch 7)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Multi-Container Pod
Create a pod named `ckad-vpiqwx` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-vpiqwx -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-vpiqwx -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 2: Multi-Container Pod
Create a pod named `ckad-xzkyb4` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-xzkyb4 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-xzkyb4 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 3: Multi-Container Pod
Create a pod named `ckad-q3vo6i` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-q3vo6i -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-q3vo6i -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 4: Multi-Container Pod
Create a pod named `ckad-rfoj8b` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-rfoj8b -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-rfoj8b -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 5: Multi-Container Pod
Create a pod named `ckad-12p6nz` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-12p6nz -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-12p6nz -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 6: Multi-Container Pod
Create a pod named `ckad-5gao3x` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-5gao3x -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-5gao3x -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 7: Multi-Container Pod
Create a pod named `ckad-adxph3` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-adxph3 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-adxph3 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 8: Multi-Container Pod
Create a pod named `ckad-8nf6of` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-8nf6of -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-8nf6of -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 9: Multi-Container Pod
Create a pod named `ckad-9in3w5` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-9in3w5 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-9in3w5 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 10: Multi-Container Pod
Create a pod named `ckad-wa19i1` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-wa19i1 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-wa19i1 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 11: Multi-Container Pod
Create a pod named `ckad-dj2m4e` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-dj2m4e -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-dj2m4e -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 12: Multi-Container Pod
Create a pod named `ckad-fr3oca` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-fr3oca -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-fr3oca -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 13: Multi-Container Pod
Create a pod named `ckad-ta96l1` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ta96l1 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ta96l1 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 14: Multi-Container Pod
Create a pod named `ckad-k4vnm3` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-k4vnm3 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-k4vnm3 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 15: Multi-Container Pod
Create a pod named `ckad-6qxdzc` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-6qxdzc -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-6qxdzc -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 16: Multi-Container Pod
Create a pod named `ckad-hyvuqs` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-hyvuqs -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-hyvuqs -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 17: Multi-Container Pod
Create a pod named `ckad-03099b` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-03099b -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-03099b -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 18: Multi-Container Pod
Create a pod named `ckad-fvbfbm` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-fvbfbm -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-fvbfbm -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 19: Multi-Container Pod
Create a pod named `ckad-tggr1d` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-tggr1d -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-tggr1d -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 20: Multi-Container Pod
Create a pod named `ckad-mrbnzu` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-mrbnzu -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-mrbnzu -o jsonpath='{.spec.containers[*].name}' | grep c2
```

