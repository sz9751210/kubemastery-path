---
id: auto-ckad-1
title: Auto CKAD Practice (Batch 1)
category: CKAD
duration: 120 mins
---

# Auto CKAD Practice (Batch 1)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Multi-Container Pod
Create a pod named `ckad-0agccb` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-0agccb -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-0agccb -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 2: Multi-Container Pod
Create a pod named `ckad-kcx9pl` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-kcx9pl -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-kcx9pl -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 3: Multi-Container Pod
Create a pod named `ckad-66eju7` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-66eju7 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-66eju7 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 4: Multi-Container Pod
Create a pod named `ckad-3djr75` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-3djr75 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-3djr75 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 5: Multi-Container Pod
Create a pod named `ckad-ny2cfa` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ny2cfa -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ny2cfa -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 6: Multi-Container Pod
Create a pod named `ckad-2prj14` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-2prj14 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-2prj14 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 7: Multi-Container Pod
Create a pod named `ckad-gr0f77` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-gr0f77 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-gr0f77 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 8: Multi-Container Pod
Create a pod named `ckad-fx5nm7` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-fx5nm7 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-fx5nm7 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 9: Multi-Container Pod
Create a pod named `ckad-v3k9o1` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-v3k9o1 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-v3k9o1 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 10: Multi-Container Pod
Create a pod named `ckad-zteimd` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-zteimd -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-zteimd -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 11: Multi-Container Pod
Create a pod named `ckad-3vr4iw` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-3vr4iw -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-3vr4iw -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 12: Multi-Container Pod
Create a pod named `ckad-re9tqo` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-re9tqo -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-re9tqo -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 13: Multi-Container Pod
Create a pod named `ckad-fs5c5m` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-fs5c5m -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-fs5c5m -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 14: Multi-Container Pod
Create a pod named `ckad-wiy29s` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-wiy29s -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-wiy29s -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 15: Multi-Container Pod
Create a pod named `ckad-oke76t` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-oke76t -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-oke76t -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 16: Multi-Container Pod
Create a pod named `ckad-bs9ekp` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-bs9ekp -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-bs9ekp -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 17: Multi-Container Pod
Create a pod named `ckad-r5x1p4` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-r5x1p4 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-r5x1p4 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 18: Multi-Container Pod
Create a pod named `ckad-n4z7fh` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-n4z7fh -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-n4z7fh -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 19: Multi-Container Pod
Create a pod named `ckad-aqku3t` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-aqku3t -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-aqku3t -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 20: Multi-Container Pod
Create a pod named `ckad-cm1n8a` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-cm1n8a -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-cm1n8a -o jsonpath='{.spec.containers[*].name}' | grep c2
```

