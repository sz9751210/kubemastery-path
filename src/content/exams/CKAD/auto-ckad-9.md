---
id: auto-ckad-9
title: Auto CKAD Practice (Batch 9)
category: CKAD
duration: 120 mins
---

# Auto CKAD Practice (Batch 9)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Multi-Container Pod
Create a pod named `ckad-bc8sui` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-bc8sui -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-bc8sui -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 2: Multi-Container Pod
Create a pod named `ckad-gscqh0` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-gscqh0 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-gscqh0 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 3: Multi-Container Pod
Create a pod named `ckad-66edp3` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-66edp3 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-66edp3 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 4: Multi-Container Pod
Create a pod named `ckad-1i9t75` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-1i9t75 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-1i9t75 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 5: Multi-Container Pod
Create a pod named `ckad-qg6mk2` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-qg6mk2 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-qg6mk2 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 6: Multi-Container Pod
Create a pod named `ckad-neokzi` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-neokzi -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-neokzi -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 7: Multi-Container Pod
Create a pod named `ckad-x71ltm` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-x71ltm -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-x71ltm -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 8: Multi-Container Pod
Create a pod named `ckad-jmzps5` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-jmzps5 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-jmzps5 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 9: Multi-Container Pod
Create a pod named `ckad-fdvb7r` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-fdvb7r -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-fdvb7r -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 10: Multi-Container Pod
Create a pod named `ckad-5znqlf` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-5znqlf -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-5znqlf -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 11: Multi-Container Pod
Create a pod named `ckad-z6t19w` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-z6t19w -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-z6t19w -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 12: Multi-Container Pod
Create a pod named `ckad-rx1g42` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-rx1g42 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-rx1g42 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 13: Multi-Container Pod
Create a pod named `ckad-1eea3v` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-1eea3v -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-1eea3v -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 14: Multi-Container Pod
Create a pod named `ckad-7uyuez` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-7uyuez -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-7uyuez -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 15: Multi-Container Pod
Create a pod named `ckad-hs670a` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-hs670a -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-hs670a -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 16: Multi-Container Pod
Create a pod named `ckad-rq9vbc` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-rq9vbc -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-rq9vbc -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 17: Multi-Container Pod
Create a pod named `ckad-o0b7sw` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-o0b7sw -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-o0b7sw -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 18: Multi-Container Pod
Create a pod named `ckad-4r5snh` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-4r5snh -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-4r5snh -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 19: Multi-Container Pod
Create a pod named `ckad-iajg7p` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-iajg7p -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-iajg7p -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 20: Multi-Container Pod
Create a pod named `ckad-tbz3u7` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-tbz3u7 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-tbz3u7 -o jsonpath='{.spec.containers[*].name}' | grep c2
```

