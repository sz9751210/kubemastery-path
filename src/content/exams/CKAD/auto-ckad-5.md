---
id: auto-ckad-5
title: Auto CKAD Practice (Batch 5)
category: CKAD
duration: 120 mins
---

# Auto CKAD Practice (Batch 5)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Multi-Container Pod
Create a pod named `ckad-4z9qro` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-4z9qro -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-4z9qro -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 2: Multi-Container Pod
Create a pod named `ckad-kzyauk` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-kzyauk -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-kzyauk -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 3: Multi-Container Pod
Create a pod named `ckad-68e9iy` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-68e9iy -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-68e9iy -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 4: Multi-Container Pod
Create a pod named `ckad-qbm15i` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-qbm15i -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-qbm15i -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 5: Multi-Container Pod
Create a pod named `ckad-kgwacj` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-kgwacj -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-kgwacj -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 6: Multi-Container Pod
Create a pod named `ckad-qkzo6g` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-qkzo6g -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-qkzo6g -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 7: Multi-Container Pod
Create a pod named `ckad-hghdxm` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-hghdxm -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-hghdxm -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 8: Multi-Container Pod
Create a pod named `ckad-g557m6` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-g557m6 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-g557m6 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 9: Multi-Container Pod
Create a pod named `ckad-f5s39e` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-f5s39e -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-f5s39e -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 10: Multi-Container Pod
Create a pod named `ckad-chkmsz` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-chkmsz -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-chkmsz -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 11: Multi-Container Pod
Create a pod named `ckad-fz04y9` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-fz04y9 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-fz04y9 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 12: Multi-Container Pod
Create a pod named `ckad-0ei7fq` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-0ei7fq -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-0ei7fq -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 13: Multi-Container Pod
Create a pod named `ckad-p1z5zv` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-p1z5zv -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-p1z5zv -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 14: Multi-Container Pod
Create a pod named `ckad-10bmu6` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-10bmu6 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-10bmu6 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 15: Multi-Container Pod
Create a pod named `ckad-yt1s9q` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-yt1s9q -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-yt1s9q -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 16: Multi-Container Pod
Create a pod named `ckad-894e9p` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-894e9p -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-894e9p -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 17: Multi-Container Pod
Create a pod named `ckad-ox26r7` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ox26r7 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ox26r7 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 18: Multi-Container Pod
Create a pod named `ckad-xxj1u8` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-xxj1u8 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-xxj1u8 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 19: Multi-Container Pod
Create a pod named `ckad-trfz2p` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-trfz2p -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-trfz2p -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 20: Multi-Container Pod
Create a pod named `ckad-edg3mc` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-edg3mc -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-edg3mc -o jsonpath='{.spec.containers[*].name}' | grep c2
```

