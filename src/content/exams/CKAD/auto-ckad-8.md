---
id: auto-ckad-8
title: Auto CKAD Practice (Batch 8)
category: CKAD
duration: 120 mins
---

# Auto CKAD Practice (Batch 8)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Multi-Container Pod
Create a pod named `ckad-8n8cv9` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-8n8cv9 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-8n8cv9 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 2: Multi-Container Pod
Create a pod named `ckad-1oh0wv` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-1oh0wv -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-1oh0wv -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 3: Multi-Container Pod
Create a pod named `ckad-xxj0p5` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-xxj0p5 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-xxj0p5 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 4: Multi-Container Pod
Create a pod named `ckad-f1sk2g` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-f1sk2g -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-f1sk2g -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 5: Multi-Container Pod
Create a pod named `ckad-v3cdtz` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-v3cdtz -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-v3cdtz -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 6: Multi-Container Pod
Create a pod named `ckad-ncslmi` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ncslmi -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ncslmi -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 7: Multi-Container Pod
Create a pod named `ckad-c13y4i` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-c13y4i -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-c13y4i -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 8: Multi-Container Pod
Create a pod named `ckad-hnf0pv` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-hnf0pv -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-hnf0pv -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 9: Multi-Container Pod
Create a pod named `ckad-4m7djx` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-4m7djx -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-4m7djx -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 10: Multi-Container Pod
Create a pod named `ckad-l501qh` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-l501qh -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-l501qh -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 11: Multi-Container Pod
Create a pod named `ckad-jzbwws` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-jzbwws -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-jzbwws -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 12: Multi-Container Pod
Create a pod named `ckad-m8hbzo` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-m8hbzo -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-m8hbzo -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 13: Multi-Container Pod
Create a pod named `ckad-wyjc6v` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-wyjc6v -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-wyjc6v -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 14: Multi-Container Pod
Create a pod named `ckad-j57t8t` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-j57t8t -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-j57t8t -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 15: Multi-Container Pod
Create a pod named `ckad-oeeb52` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-oeeb52 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-oeeb52 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 16: Multi-Container Pod
Create a pod named `ckad-8pvbt8` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-8pvbt8 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-8pvbt8 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 17: Multi-Container Pod
Create a pod named `ckad-q5574o` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-q5574o -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-q5574o -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 18: Multi-Container Pod
Create a pod named `ckad-1p45u9` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-1p45u9 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-1p45u9 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 19: Multi-Container Pod
Create a pod named `ckad-nyzpvu` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-nyzpvu -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-nyzpvu -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 20: Multi-Container Pod
Create a pod named `ckad-hgyaj0` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-hgyaj0 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-hgyaj0 -o jsonpath='{.spec.containers[*].name}' | grep c2
```

