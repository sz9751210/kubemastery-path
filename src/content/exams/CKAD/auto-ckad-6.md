---
id: auto-ckad-6
title: Auto CKAD Practice (Batch 6)
category: CKAD
duration: 120 mins
---

# Auto CKAD Practice (Batch 6)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Multi-Container Pod
Create a pod named `ckad-s16o1l` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-s16o1l -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-s16o1l -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 2: Multi-Container Pod
Create a pod named `ckad-9q3q4u` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-9q3q4u -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-9q3q4u -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 3: Multi-Container Pod
Create a pod named `ckad-cbq3kl` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-cbq3kl -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-cbq3kl -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 4: Multi-Container Pod
Create a pod named `ckad-tr1i4f` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-tr1i4f -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-tr1i4f -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 5: Multi-Container Pod
Create a pod named `ckad-m39pac` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-m39pac -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-m39pac -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 6: Multi-Container Pod
Create a pod named `ckad-7iwqhp` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-7iwqhp -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-7iwqhp -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 7: Multi-Container Pod
Create a pod named `ckad-je7stu` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-je7stu -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-je7stu -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 8: Multi-Container Pod
Create a pod named `ckad-f96bwp` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-f96bwp -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-f96bwp -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 9: Multi-Container Pod
Create a pod named `ckad-0vfcbb` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-0vfcbb -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-0vfcbb -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 10: Multi-Container Pod
Create a pod named `ckad-f3rk8l` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-f3rk8l -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-f3rk8l -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 11: Multi-Container Pod
Create a pod named `ckad-bwuj3r` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-bwuj3r -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-bwuj3r -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 12: Multi-Container Pod
Create a pod named `ckad-n75wi5` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-n75wi5 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-n75wi5 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 13: Multi-Container Pod
Create a pod named `ckad-2mhf34` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-2mhf34 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-2mhf34 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 14: Multi-Container Pod
Create a pod named `ckad-5h8pfd` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-5h8pfd -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-5h8pfd -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 15: Multi-Container Pod
Create a pod named `ckad-irdk5l` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-irdk5l -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-irdk5l -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 16: Multi-Container Pod
Create a pod named `ckad-drtowv` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-drtowv -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-drtowv -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 17: Multi-Container Pod
Create a pod named `ckad-7g7b7a` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-7g7b7a -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-7g7b7a -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 18: Multi-Container Pod
Create a pod named `ckad-eqmd2l` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-eqmd2l -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-eqmd2l -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 19: Multi-Container Pod
Create a pod named `ckad-pvpq3b` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-pvpq3b -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-pvpq3b -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 20: Multi-Container Pod
Create a pod named `ckad-rbyvic` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-rbyvic -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-rbyvic -o jsonpath='{.spec.containers[*].name}' | grep c2
```

