---
id: auto-ckad-15
title: Auto CKAD Practice (Batch 15)
category: CKAD
duration: 120 mins
---

# Auto CKAD Practice (Batch 15)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Multi-Container Pod
Create a pod named `ckad-oi5xjn` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-oi5xjn -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-oi5xjn -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 2: Multi-Container Pod
Create a pod named `ckad-d4u05s` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-d4u05s -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-d4u05s -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 3: Multi-Container Pod
Create a pod named `ckad-aj05cg` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-aj05cg -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-aj05cg -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 4: Multi-Container Pod
Create a pod named `ckad-gdo27n` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-gdo27n -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-gdo27n -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 5: Multi-Container Pod
Create a pod named `ckad-qb7o2m` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-qb7o2m -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-qb7o2m -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 6: Multi-Container Pod
Create a pod named `ckad-ca25w2` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ca25w2 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ca25w2 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 7: Multi-Container Pod
Create a pod named `ckad-52ak0p` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-52ak0p -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-52ak0p -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 8: Multi-Container Pod
Create a pod named `ckad-a9t9m0` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-a9t9m0 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-a9t9m0 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 9: Multi-Container Pod
Create a pod named `ckad-50gynt` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-50gynt -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-50gynt -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 10: Multi-Container Pod
Create a pod named `ckad-qjls7a` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-qjls7a -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-qjls7a -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 11: Multi-Container Pod
Create a pod named `ckad-pekup8` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-pekup8 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-pekup8 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 12: Multi-Container Pod
Create a pod named `ckad-ao6rtx` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ao6rtx -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ao6rtx -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 13: Multi-Container Pod
Create a pod named `ckad-48ekjt` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-48ekjt -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-48ekjt -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 14: Multi-Container Pod
Create a pod named `ckad-5kky32` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-5kky32 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-5kky32 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 15: Multi-Container Pod
Create a pod named `ckad-kmskan` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-kmskan -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-kmskan -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 16: Multi-Container Pod
Create a pod named `ckad-hiz92s` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-hiz92s -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-hiz92s -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 17: Multi-Container Pod
Create a pod named `ckad-ai1xxv` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-ai1xxv -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-ai1xxv -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 18: Multi-Container Pod
Create a pod named `ckad-hf201n` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-hf201n -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-hf201n -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 19: Multi-Container Pod
Create a pod named `ckad-un8hm2` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-un8hm2 -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-un8hm2 -o jsonpath='{.spec.containers[*].name}' | grep c2
```


# Task 20: Multi-Container Pod
Create a pod named `ckad-wij0nc` with two containers.
Container 1: image `nginx`, name `c1`.
Container 2: image `busybox`, name `c2`, command "sleep 3600".

```verify
kubectl get pod ckad-wij0nc -o jsonpath='{.spec.containers[*].name}' | grep c1
kubectl get pod ckad-wij0nc -o jsonpath='{.spec.containers[*].name}' | grep c2
```

