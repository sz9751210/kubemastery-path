---
id: auto-cks-7
title: Auto CKS Practice (Batch 7)
category: CKS
duration: 120 mins
---

# Auto CKS Practice (Batch 7)

This is an auto-generated exam to provide infinite practice questions.

# Task 1: Network Policy
Create a NetworkPolicy named `cks-98r847` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 2: Network Policy
Create a NetworkPolicy named `cks-3afybq` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 3: Network Policy
Create a NetworkPolicy named `cks-q0cdnz` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 4: Network Policy
Create a NetworkPolicy named `cks-dyzyt3` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 5: Network Policy
Create a NetworkPolicy named `cks-wt6s7s` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 6: Network Policy
Create a NetworkPolicy named `cks-x0aw91` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 7: Network Policy
Create a NetworkPolicy named `cks-ppw6qw` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 8: Network Policy
Create a NetworkPolicy named `cks-tk48k8` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 9: Network Policy
Create a NetworkPolicy named `cks-8alhqo` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 10: Network Policy
Create a NetworkPolicy named `cks-ihc5ts` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 11: Network Policy
Create a NetworkPolicy named `cks-xwdh41` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 12: Network Policy
Create a NetworkPolicy named `cks-f5r0k4` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 13: Network Policy
Create a NetworkPolicy named `cks-qoyti4` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 14: Network Policy
Create a NetworkPolicy named `cks-8a9zl3` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 15: Network Policy
Create a NetworkPolicy named `cks-k1b3yq` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 16: Network Policy
Create a NetworkPolicy named `cks-7497a3` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 17: Network Policy
Create a NetworkPolicy named `cks-m1a4v5` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 18: Network Policy
Create a NetworkPolicy named `cks-9wwdyd` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 19: Network Policy
Create a NetworkPolicy named `cks-x3fle2` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).


# Task 20: Network Policy
Create a NetworkPolicy named `cks-t015d9` in namespace `default`.
Deny all ingress traffic to pods with label `role=db`.
Allow egress only to port 53 (DNS).

